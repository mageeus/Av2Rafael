@model IEnumerable<Jogos.Model.Models.JogosCategoria>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Categoria)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Jogo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Categoria.CategoriaId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Jogo.JogoId)
            </td>
            <td>
                @*<a asp-action="Edit" asp-route-id="@item.JogoCategoriaId">Edit</a> |*@
                @*<a asp-action="Details" asp-route-id="@item.JogoCategoriaId">Details</a> |*@
                @*<a asp-action="Delete" asp-route-id="@item.JogoCategoriaId">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
